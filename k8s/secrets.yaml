apiVersion: v1
kind: Secret
metadata:
  name: supabase-secrets
  namespace: noclue-app
type: Opaque
stringData:
  # These will be populated by GitHub Actions from GitHub Secrets
  # DO NOT commit actual values here
  SUPABASE_URL: "REPLACE_IN_CICD"
  SUPABASE_SECRET_KEY: "REPLACE_IN_CICD"
  SUPABASE_PUBLISHABLE_KEY: "REPLACE_IN_CICD"
