apiVersion: v1
kind: Service
metadata:
  name: llm-service
  labels:
    app: llm-service
spec:
  type: ClusterIP
  selector:
    app: llm-service
  ports:
    - port: 4005
      targetPort: 4005
      protocol: TCP
      name: http
  sessionAffinity: None

